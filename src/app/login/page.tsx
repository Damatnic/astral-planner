import LoginClient from './LoginClient';

export const metadata = {
  title: 'Sign In - Astral Chronos',
  description: 'Sign in to your Astral Chronos digital planner account',
};

export default function LoginPage() {
  return <LoginClient />;
}